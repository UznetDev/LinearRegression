# linear_regression/__init__.py

# Import the `LinearRegression` class from the `model.py` file.
from .model import LinearRegression

# Now, when users import the `linear_regression` package,
# they can directly use the `LinearRegression` class:
# For example: `from linear_regression import LinearRegression`
